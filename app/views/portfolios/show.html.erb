<div class="container portfolio-container">
  <div class="row">
    <div class="col-md-7"
      <%= image_tag portfolio_img(@portfolio_item.main_image, 'main'), width: '100%' %>
    </div>

    <div>
      <h1><%= @portfolio_item.title %></h1>

      <em><%= @portfolio_item.subtitle %></em>

      <p><%= @portfolio_item.body %></p>

      <%= image_tag portfolio_img(@portfolio_item.main_image, 'main'), width: '90%' %>

      <h2>Technologies Used:</h2>

      <% @portfolio_item.technologies.each do |t| %>
      <p><%= t.name %></p>
      <% end %>

      <%= link_to 'Back', portfolios_path %>

      <% if logged_in?(:site_admin) %>
        <div class="col-md-12">
          <%= link_to "Edit Item", edit_portfolio_path(@portfolio_item), class: 'btn btn-warning' %>
          <%= link_to "Delete Post", portfolio_path(@portfolio_item), method: :delete, class: 'btn btn-warning', data: {confirm: "Are you sure?"} %>
        </div>
      <% end %>

    </div>
  </div>
</div>