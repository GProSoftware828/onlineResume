<div class="card">
  <div class="title-s">
    <h4>Small sample of blogs from previous personal website:</h4>
    <h5>Listen to one of my personal rumination notes for a quick second</h5>
  </div>
  <%= link_to 'New', new_blog_url %>
  <%= source_helper("alert alert-warning") %>
  <div class="blog-group">
    <div class=" row <%= "sortable" if logged_in?(:site_admin) %>">
      <%= render @blogs %>
    </div>
  </div>
</div>